git的工作流程：
1、将本地文件夹及内容同步到线上文件夹
（1）查找变动 （2）staged changes （3）commit （4）push
2、将线上文件夹及内容同步到本地文件夹
（1）fetch （2）merge
我的理解：
1、将本地文件夹及内容同步到线上文件夹
修改本地文件后，由工作区通过git查找到变动，将修改后的文件存放在暂存区，提交更改信息后Git将文件存在本地仓库，再将文件从本地仓库push到远程
仓库。
2、将线上文件夹及内容同步到本地文件夹
修改线上文件夹后，git从远程仓库获取到变动，并将变动传到本地仓库（fetch），再将文件从本地仓库传到工作区（merge）。
